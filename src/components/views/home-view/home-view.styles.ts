import { SxProps } from '@mui/material';

export const useHomeViewStyles = () => {
  const wrapper: SxProps = {
    minHeight: '100vh',
    display: 'flex',
    width: '100%',
  };

  return {
    wrapper,
  };
};
